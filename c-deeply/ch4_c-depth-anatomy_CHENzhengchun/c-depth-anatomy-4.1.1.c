#include <stdio.h>

int main()
{
	int * p;

	printf("The size of (int *) is: %d.\n", sizeof(int *));
	printf("The size of (void *) is: %d.\n", sizeof(void *));
	/* Print the sizes of (int *) & (void *). */

	return 0;
}
